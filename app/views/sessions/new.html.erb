<!DOCTYPE html>
<html lang="es" >
  <head>
    <title>Login</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>

  <body style="background-color: #eee">

    <div class="container" >

      <%= form_for :session, url: ingresar_path, html: {class: "form-signin"} do |f| %>
        <div class="center">
          <%= link_to image_tag("RD-logo.png") %>

          <% flash.each do |key, value| %>
            <div class="form-control alert alert-<%= key %> <%= "alert-danger" if key == "error" %> " role="alert">
              <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Cerrar</span></button>
              <%= value %>
            </div>
          <% end %>
          <h2 class="form-signin-heading"><%= t(:signin).humanize %></h2>
        </div>

        <%= f.text_field :email, class: "form-control", placeholder: t(:email).humanize %>
        <%= f.password_field :password, class: "form-control", placeholder: t(:password).humanize %>
        <%= link_to "Olvido contraseña?", new_password_reset_path %>
        <%= f.label :remember_me, class: "control-label inline" do %>
          <%= f.check_box :remember_me %>
          <span>Recordarme en este computador</span>
        <% end %>
        <%= f.submit t(:signin).humanize, class: "btn btn-lg btn-primary btn-block" %>
      <% end %>


      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div> <!-- /container -->

  </body>
</html>

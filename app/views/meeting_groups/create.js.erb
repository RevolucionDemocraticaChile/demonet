<% if @success %>
  $("#organizing_groups_ul").append("<%= escape_javascript(render partial: 'meetings/organizing_group_li', locals: {meeting_group: @meeting_group, removable: true}) %>");
  $("h1").html("<%= format_meeting_name(@meeting_group.meeting) %>");
<% else %>
  alert("Error");
<% end %>

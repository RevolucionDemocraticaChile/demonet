<% if @success %>
  $("#attendants_ul").append("<%= escape_javascript(render partial: 'meetings/attendant_li', locals: {user_meeting: @user_meeting, removable: true}) %>");
  // $('option[value=' + <%= @user_meeting.user_id %> + ']').prop('disabled', true);
<% else %>
  alert("Error");
<% end %>

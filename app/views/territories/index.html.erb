<div class="row">
  <div class="col-lg-6 col-lg-offset-3">
    <h1>Territorios</h1>

    <table class="table table-striped table-bordered">
      <thead>
        <tr>
          <th>Nombre</th>
          <th class="text-center">Miembros</th>
          <th class="text-center">Actas</th>
          <% if current_user.admin? %>
            <th colspan="2"></th>
          <% end %>
        </tr>
      </thead>

      <tbody>
        <% @territories.each do |territory| %>
          <tr>
            <td><%= link_to territory.name, territory %></td>
            <td class="text-center"><%= territory.members.count %></td>
            <td class="text-center"><%= territory.meetings.count %></td>
            <% if current_user.admin? %>
              <td class="text-center">
                <%= link_to 'Editar', edit_territory_path(territory), class: "btn btn-info" %>
              </td>
              <td class="text-center">
                <%= link_to 'Eliminar', territory, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
              </td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>

    <% if can? :new, Territory %>
      <%= link_to 'Nuevo Territorio', new_territory_path, class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
